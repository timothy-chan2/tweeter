{
  "name": "tweeter",
  "version": "1.0.0",
  "description": "A twitter clone single page app built with HTML, CSS, JS, jQuery and AJAX",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "local": "./node_modules/.bin/nodemon --watch server -e js server/index.js",
    "sass-watch": "sass sass:public/styles --watch --poll --no-source-map"
  },
  "contributors": [
    "Timothy Chan (timothy.chan2@mail.mcgill.ca)",
    "Lighthouse Labs"
  ],
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "chance": "^1.0.2",
    "express": "^4.13.4",
    "md5": "^2.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.9.2"
  }
}
